<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- A torrent takes hours. Android suspends an app the moment you leave it, so without a
         foreground service a download stops when you check a message. dataSync is the correct
         type for "transferring a file the user asked for"; Android 14 requires the subtype to
         be declared or the service throws on start. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- The notification IS the permission slip for running in the background, and since
         Android 13 the user has to grant it. Without it the service still runs, but silently. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Keeps the CPU alive through Doze while bytes are actually moving. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- All files access, so downloads land in the real Downloads folder.
         The alternatives do not work for this app. MediaStore and the Storage Access Framework
         both hand out content:// URIs, and librqbit writes filesystem paths — adapting it means
         a custom storage backend in Rust. The app-specific folder needs no permission, but on
         Android 11+ Google hid Android/data from the Files app, so a finished download is there
         and unreachable, which is exactly the bug this replaces.
         Play Store restricts this permission; flai is sideloaded, so it does not apply. Every
         serious Android torrent client asks for the same thing. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <!-- Pre-Android-11 devices, where all-files access does not exist yet. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.flai_app"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Holds the process alive while downloads run. It owns no torrent state: the engine
             lives in the Rust library in this same process, so the service's only job is to
             tell Android "this process is doing something the user asked for". -->
        <service
            android:name=".DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
